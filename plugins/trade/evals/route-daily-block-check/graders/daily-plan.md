---
type: llm
---

Context: the user asked whether NBIS had any big block orders (大单) today. No market data could be pulled in this session.

PASS if the reply mentions both of the following as part of how it would read NBIS's day once data is available. Any wording and either language counts, and a list or table counts:
- Comparing today's volume or options volume with NBIS's own recent average, for example against its 30-day average (an activity check).
- Filtering the large prints before reading direction from them, for example removing multi-leg spreads, removing trades with no aggressor side, or removing volume from corporate actions such as a convertible offering.

FAIL if either item is missing.
